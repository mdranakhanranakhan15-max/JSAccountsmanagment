# JS NETWORK — environment configuration
# Copy this file as .env and fill in the cPanel MySQL credentials
# (cPanel > Databases > MySQL Databases > "Add User to Database").

# Web server port (most cPanel Node.js hosts inject this; 3000 is the dev default)
PORT=3000

DB_HOST=localhost
DB_USER=jsnetwor_accounts
DB_PASSWORD=jJ396580jJ
DB_NAME=jsnetwor_accounts